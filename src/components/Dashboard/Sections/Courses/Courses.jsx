import React from "react";

const Courses = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-6" style={{ color: "var(--color-text-base)" }}>
        Courses
      </h1>
      <div className="p-6 rounded-lg shadow" style={{ backgroundColor: "white" }}>
        <p style={{ color: "var(--color-text-base)" }}>This would show Courses Cards when connected to real data.</p>
      </div>
    </div>
  );
};

export default Courses;
